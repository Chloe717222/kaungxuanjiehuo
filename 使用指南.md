# 🧭 框选解惑 · 从零开始配置指南

> **献给第一次使用浏览器扩展的朋友**  
> 这份指南假设你什么都不知道，每一步都说得清清楚楚。跟着做就行。

---

## 📖 这是个什么东西？

**「框选解惑」** 是一个安装在浏览器里的小工具（叫"浏览器扩展"）。

它的作用是：**你在网页上看到任何看不懂的文字，选中它 → 点一下 → AI 用大白话给你解释 → 还能一键保存成笔记。**

举个例子：
- 你在英文文章里看到 `quantum entanglement`，不理解 → 选中它 → 弹窗告诉你"量子纠缠就是两个粒子无论隔多远都会互相影响，像心有灵犀的双胞胎"
- 你在新闻里看到"去杠杆化" → 选中它 → 弹窗告诉你"就是把欠的债还了，减少负债"

安装后它叫 **「框选解惑」**，里面帮你解释的 AI 助手叫 **「惑惑」**（专治各种"这是什么？"）。

---

## 第一部分：把扩展装进浏览器

### ▎你需要先准备什么

- 一台电脑（Windows 或 Mac 都可以）
- 一个浏览器：推荐 **Microsoft Edge** 或 **Google Chrome**

### ▎如果你用 Edge（推荐）

**第 1 步**：打开 Edge 浏览器，在顶部地址栏输入：

```
edge://extensions
```

然后按键盘上的 **Enter** 回车键。

**第 2 步**：你会看到一个扩展管理页面。在**左下角**找到一个开关，写着：

```
开发人员模式
```

点一下这个开关，让它变成**打开**状态（蓝色或高亮）。

**第 3 步**：页面左上角会出现几个新按钮，点：

```
加载解压缩的扩展
```

**第 4 步**：浏览器会弹出一个文件选择窗口。找到存放扩展代码的那个文件夹，选中它，点「选择文件夹」。

> 💡 如果你不知道文件夹在哪，看看桌面有没有叫 `kuangxuanjiehuo` 的文件夹，或者问给你分享这个扩展的朋友要一下文件夹路径。

**第 5 步**：加载成功后，扩展列表里会出现 **「框选解惑」**。浏览器右上角工具栏会出现一个蓝色圆形带问号的图标 🔍。

✅ **扩展安装完成！**

---

### ▎如果你用 Chrome

**第 1 步**：打开 Chrome 浏览器，在顶部地址栏输入：

```
chrome://extensions
```

按 **Enter** 回车。

**第 2 步**：在页面右上角找到开关：

```
开发者模式
```

点一下让它**打开**。

**第 3 步**：点左上角的：

```
加载已解压的扩展程序
```

**第 4 步**：选择扩展文件夹 → 点「选择文件夹」。

**第 5 步**：右上角出现蓝色图标 ✅

---

## 第二部分：配置 AI 服务（最关键的一步）

扩展安装后还不能直接用——它需要一个 AI 大脑来帮你解释。你需要给它配一个 **API Key**（相当于通行证）。

### ▎听不懂没关系，你只需要做两件事

**① 去 AI 服务商注册 → ② 把密钥复制过来粘贴进去**

下面以 **DeepSeek** 为例（最便宜、最简单，注册送 500 万 tokens，够用很久）：

### ▎详细操作

**第 1 步**：打开 DeepSeek 官网，点右上角「登录/注册」

```
https://platform.deepseek.com
```

用你的手机号或邮箱注册。

**第 2 步**：注册登录后，在左侧菜单找到并点：

```
API Keys
```

**第 3 步**：点页面中间的：

```
创建 API Key
```

**第 4 步**：随便起个名字（比如"框选解惑"），点确认。

**第 5 步**：系统会生成一串以 `sk-` 开头的密钥。**立即复制它！**（旁边有个复制按钮，点一下）

> ⚠️ 这个密钥只显示一次，关掉页面就看不到了。如果忘了，可以删除旧的重新创建一个新的。

**第 6 步**：**充值**（DeepSeek 需要先充值才能用）

在 DeepSeek 左侧菜单找到「充值」或「财务管理」，充 **10 块钱**就够了，可以用非常久。

---

### ▎把密钥填到扩展里

**第 1 步**：点击浏览器右上角的 **蓝色圆形图标** 🔍

会弹出一个配置面板。

**第 2 步**：在面板里找到「API Key」输入框，把你刚才复制的 `sk-xxxxxxxx` 粘贴进去（按 **Ctrl+V** 或右键 → 粘贴）。

**第 3 步**：确认服务商选的是「DeepSeek」（默认就是）。

**第 4 步**：点底下的：

```
保存并启用
```

看到提示「配置已保存」就成功了！

✅ **AI 配置完成！现在可以开始使用了。**

---

## 第三部分：怎么用

### ▎最基础的操作：框选文字看解释

**第 1 步**：随便打开一个网页（比如新闻、百度百科、英文文章）。

**第 2 步**：用鼠标选中一段你不理解的文字。

- 比如选中「机器学习」
- 或者选中一个英文单词 `photosynthesis`

**第 3 步**：选中后，文字旁边/结尾会出现一个 **🔍 蓝色的图标**，用鼠标**点击它**。

**第 4 步**：稍等 2-5 秒，会弹出一个窗口，惑惑就会用大白话给你解释。

**第 5 步**：看完解释，点弹窗右上角的 **✕** 关闭。

---

### ▎怎么关闭弹窗

- 点弹窗右上角的 **✕** 按钮
- 或者**连续两次点击**弹窗外面的区域（防止误触）

---

### ▎常见情况

| 情况 | 怎么做 |
|------|--------|
| 选中文字后没出现图标 | 换一个网页试试点，有些特殊页面不支持（比如浏览器设置页） |
| 选中空白处没反应 | 必须选中**有文字**的地方 |
| 弹窗一直转圈 | 检查 API Key 是否填对、账号是否已充值 |

---

## 第四部分：把解释保存为笔记（可选但推荐）

每次看到有用的解释，可以一键存到笔记软件里。

### ▎如果你用 Obsidian

**Obsidian 是一个免费的笔记软件**，可以把自己的知识整理成笔记库。

**第 1 步**：下载安装 Obsidian（从 obsidian.md 官网下载）。

**第 2 步**：打开 Obsidian，**创建一个知识库**（名字随便取，比如"我的学习"）。

**第 3 步**：安装连接插件：

Obsidian 左侧点 **齿轮图标 ⚙️**（设置）→ **第三方插件** → 点 **「关闭安全模式」**

点 **「浏览」** → 在搜索框里输入：

```
Local REST API
```

找到后点 **安装** → **启用**。

**第 4 步**：开启 HTTP 通道并获取密钥：

在插件列表找到 **Local REST API** → 点右边的 **齿轮图标 ⚙️** → 点 **「Show advanced settings」**

找到 **「Enable non-encrypted (HTTP) server」** → 确保**勾选**（打上 √）

在它下方找到 **「Non-encrypted (HTTP) server port 27123」** → 下面有一个 **API key** → **复制它**。

> 💡 不用点「Create new key」，Key 已经自动生成好了，直接复制就行。

**第 5 步**：把密钥填到扩展的设置里：

在扩展图标上 **右键** → **选项** → 找到「API Key」下面的输入框 → 粘贴密钥 → 点「保存设置」。

确保 REST API 地址是 `http://127.0.0.1:27123`（默认就是，一般不用改）。

以后使用的时候要**保持 Obsidian 开着**才能保存。

**第 6 步**：在弹窗底部点 **「📡 同步到知识库」**，解释就会自动存到 Obsidian 里。

---

### ▎不装笔记软件也能用

即使不用 Obsidian，你还有两种保存方式：

- **📥 下载文件** — 把解释保存成一个 `.md` 文件（可以用记事本打开）
- **📋 复制内容** — 复制到剪贴板，然后粘贴到任何地方（微信、飞书、备忘录）

---

## 第五部分：更多技巧

### 🔑 快捷键 Ctrl+Q

在任何网页按 **Ctrl+Q**（Mac 按 MacCtrl+Q）可以直接打开对话窗口，随便问问题。

### 💬 在弹窗里追问

看完解释后还可以继续问：「再详细说说」「举个例子」「这个和 XX 有什么区别？」

### 📁 保存到指定文件夹

弹窗底部有一个文件夹输入框，填上文件夹名字就可以把笔记保存到知识库的指定位置。

---

## 第六部分：常见问题

### ❓ 加载扩展时提示"清单文件无效"或报错

检查是否选对了文件夹——要选里面包含 `manifest.json` 的那个文件夹，而不是它的上一层。

### ❓ 保存到知识库提示失败

- 确认 Obsidian 是**打开状态**
- 确认 **Local REST API** 插件已启用
- 确认插件设置里 **「Enable non-encrypted (HTTP) server」已勾选**
- 确认 API Key 粘贴正确（是 Advanced settings 下面 HTTP server 的 Key，不是顶部的 Bearer Key）
- 确认 REST API 地址是 `http://127.0.0.1:27123`

### ❓ 解释是英文的

系统提示词是中文的，如果出现英文，多试几次。也可以追问一句「用中文解释」。

### ❓ 怎么换 AI 服务商

右键图标 → 选项 → 在服务商那里可以选 OpenAI（需要你有 OpenAI 的 API Key）或自定义（兼容任何 OpenAI 接口的服务）。

### ❓ 怎么找回我之前保存的笔记

在 Obsidian 左侧文件列表里，找到你保存时指定的文件夹，所有笔记都在那里。

---

## 最后

如果你卡在任何一步，把问题描述清楚发给分享给你这个扩展的朋友。实在不行，从头再读一遍指南，慢慢来，每个按钮都点一下试试，不会弄坏电脑的 😊
